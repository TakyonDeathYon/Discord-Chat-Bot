# Discord-Chat-Bot

This implements a simple neural network that can perform simple conversation on discord. This is done through classification of 
the input, and this matching this to a set of pre-loaded outputs. The inputs and outputs are handled using discords api, allowing the chat bot to 
interact with users on discord servers.
